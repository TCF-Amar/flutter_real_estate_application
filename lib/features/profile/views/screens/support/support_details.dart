import 'package:flutter/material.dart';

class SupportDetails extends StatelessWidget {
  const SupportDetails({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text("Support Details")));
  }
}
